# flutter_api_application

A Flutter project that connects to an API and displays the data for the user

## Main Packages
- get [for state management and routing]
- shimmer [for nice UI while the data loads]
